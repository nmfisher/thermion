name: thermion_web_gallery
description: Live WebAssembly gallery of the Thermion Dart examples. One WASM build dispatches to any example via ?example=NAME.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.3.0

dependencies:
  thermion_dart:
    path: ../../../thermion_dart
  thermion_examples_lib:
    path: ../examples_lib
  logging: ^1.3.0
  web: ^1.0.0
