// filament::utils::Entity is the core C++ Filament type used to represent a
// handle for lights, renderables, cameras, etc. In Dart, we represent this
// with the ThermionEntity type.
typedef ThermionEntity = int;
final ThermionEntity FILAMENT_ENTITY_NULL = 0;
