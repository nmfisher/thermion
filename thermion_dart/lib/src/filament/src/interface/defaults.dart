import '../../filament.dart';

const kDefaultAxisColors = const <LinearColor>[
  const LinearColor(1.0, 0, 0),
  const LinearColor(0.0, 1.0, 0),
  const LinearColor(0.0, 0.0, 1),
];
const kDefaultGridColor = const LinearColor(0.3, 0.35, 0.3);
