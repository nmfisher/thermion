material {
    name : Wireframe,
    parameters : [
        {
            type: float4,
            name: edgeColor,
            default: [1.0, 1.0, 1.0, 1.0]
        },
        {
            type: float4,
            name: faceColor,
            default: [0.2, 0.2, 0.2, 0.3]
        },
        {
            type: float,
            name: edgeWidth,
            default: 3.0
        }
    ],
    requires : [
        custom0
    ],
    variables : [
        barycentrics
    ],
    depthWrite : true,
    depthCulling : true,
    doubleSided : false,
    shadingModel : unlit,
    blending : opaque,
    culling : back
}

vertex {
    void materialVertex(inout MaterialVertexInputs material) {
        material.barycentrics = getCustom0();
    }
}

fragment {
    void material(inout MaterialInputs material) {
        prepareMaterial(material);

        vec3 bary = variable_barycentrics.xyz;
        vec3 fw = fwidth(bary);
        float lineWidthPixels = materialParams.edgeWidth;
        vec3 threshold = fw * lineWidthPixels;
        vec3 edgeDist = smoothstep(vec3(0.0), threshold, bary);
        float interior = min(min(edgeDist.x, edgeDist.y), edgeDist.z);

        vec3 edgeColor = materialParams.edgeColor.rgb;
        vec3 faceColor = materialParams.faceColor.rgb;

        vec3 color = mix(edgeColor, faceColor, interior);
        material.baseColor = vec4(color, 1.0);
    }
}
